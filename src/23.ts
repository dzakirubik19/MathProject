// Example of a simple counter that increments each time it is called
let count = 0;

function incrementCounter() {
    count++;
}

incrementCounter();
incrementCounter();

console.log(count); // Output: 1

incrementCounter();
incrementCounter();

console.log(count); // Output: 2
